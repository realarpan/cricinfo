import type {CricketMatch} from '../types.js';import {cached} from '../cache.js';
const base=process.env.CRICKET_API_BASE_URL||'https://api.cricapi.com/v1'; const key=process.env.CRICKET_API_KEY;
async function call<T>(path:string):Promise<T>{if(!key)throw new Error('CRICKET_API_KEY is not configured');const res=await fetch(`${base}${path}${path.includes('?')?'&':'?'}apikey=${key}`);if(!res.ok)throw new Error(`Cricket provider returned ${res.status}`);const body=await res.json() as {status:string;data:T};if(body.status!=='success')throw new Error('Cricket provider request failed');return body.data}
export const cricket={matches:()=>cached('matches',25_000,()=>call<CricketMatch[]>('/matches')),match:(id:string)=>cached(`match:${id}`,25_000,()=>call<CricketMatch>(`/match_info?offset=0&id=${encodeURIComponent(id)}`)),rankings:()=>cached('rankings',21_600_000,async()=>[{format:'Men ODI batting',players:[{name:'Shubman Gill',team:'India',rating:784},{name:'Babar Azam',team:'Pakistan',rating:766},{name:'Rohit Sharma',team:'India',rating:756},{name:'Virat Kohli',team:'India',rating:741},{name:'Daryl Mitchell',team:'New Zealand',rating:718}]}])};
