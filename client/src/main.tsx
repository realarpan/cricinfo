import React,{Component} from 'react';import {createRoot} from 'react-dom/client';import {BrowserRouter,Route,Routes} from 'react-router-dom';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import './index.css';import {Layout} from './components/Layout';import {Home} from './pages/Home';import {MatchPage} from './pages/Match';import {SeriesPage,TeamPage} from './pages/Explore';import {AdminPage,NewsPage,SchedulePage,SearchPage} from './pages/Utility';
class Boundary extends Component<{children:React.ReactNode},{bad:boolean}>{state={bad:false};static getDerivedStateFromError(){return {bad:true}}render(){return this.state.bad?<main className="p-8">Something went wrong. Please reload this page.</main>:this.props.children}}
const qc=new QueryClient({defaultOptions:{queries:{staleTime:20_000,retry:1}}});
if(localStorage.theme==='dark'||(!localStorage.theme&&matchMedia('(prefers-color-scheme: dark)').matches))document.documentElement.classList.add('dark');
if('serviceWorker' in navigator) window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js'));
createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={qc}><Boundary><BrowserRouter><Routes><Route element={<Layout/>}><Route index element={<Home/>}/><Route path="match/:id" element={<MatchPage/>}/><Route path="news" element={<NewsPage/>}/><Route path="search" element={<SearchPage/>}/><Route path="schedule" element={<SchedulePage/>}/><Route path="teams/:slug" element={<TeamPage/>}/><Route path="series/:slug" element={<SeriesPage/>}/><Route path="admin" element={<AdminPage/>}/></Route></Routes></BrowserRouter></Boundary></QueryClientProvider></React.StrictMode>);
