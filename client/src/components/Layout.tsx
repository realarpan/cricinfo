import { useState } from 'react';
import { Link, NavLink, Outlet } from 'react-router-dom';
import { Moon, Search, Sun, Menu, X } from 'lucide-react';
const nav = [['/', 'Scores'], ['/news', 'News'], ['/teams/india', 'Teams'], ['/series/ipl', 'Series'], ['/schedule', 'Schedule']];
export function Layout() { const [open,setOpen]=useState(false); const [dark,setDark]=useState(localStorage.theme === 'dark'); const toggle=()=>{const v=!dark;setDark(v);document.documentElement.classList.toggle('dark',v);localStorage.theme=v?'dark':'light'}; return <><header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur dark:border-slate-800 dark:bg-slate-950/95"><div className="mx-auto flex h-16 max-w-7xl items-center gap-5 px-4"><Link className="text-xl font-black tracking-tight text-grass" to="/">STUMP<span className="text-pitch dark:text-white">STREAM</span></Link><nav className="hidden gap-5 md:flex">{nav.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`text-sm font-semibold ${isActive?'text-grass':'text-slate-500 hover:text-slate-900 dark:hover:text-white'}`}>{label}</NavLink>)}</nav><div className="ml-auto flex items-center gap-2"><Link aria-label="Search" to="/search" className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><Search size={19}/></Link><button aria-label="Toggle theme" onClick={toggle} className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-slate-800">{dark?<Sun size={19}/>:<Moon size={19}/>}</button><button aria-label="Menu" onClick={()=>setOpen(!open)} className="p-2 md:hidden">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="flex flex-col gap-3 border-t p-4 md:hidden">{nav.map(([to,label])=><Link key={to} to={to} onClick={()=>setOpen(false)}>{label}</Link>)}</nav>}</header><main className="mx-auto min-h-[calc(100vh-64px)] max-w-7xl px-4 py-6"><Outlet/></main><footer className="border-t border-slate-200 py-8 text-center text-sm text-slate-500 dark:border-slate-800">© {new Date().getFullYear()} StumpStream · Independent cricket coverage</footer></> }
